controller:
  hostNetwork: true
  dnsPolicy: ClusterFirstWithHostNet
  metrics:
    enabled: true
    service:
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "10254"
  admissionWebhooks:
    enabled: false
  service:
    type: ClusterIP
